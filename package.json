{
  "name": "traiter-player",
  "version": "0.1.0",
  "description": "A web-based player for Traiter stories.",
  "main": "index.js",
  "scripts": {
    "build": "rm -f dist/* && npx esbuild --bundle player/index.ts --outdir=dist --sourcemap && cp -R static/* dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": {
    "name": "Nat Knight",
    "email": "nathaniel.ep@gmail.com",
    "url": "https://nathanielknight.ca"
  },
  "devDependencies": {
    "esbuild": "^0.13.3",
    "typescript": "^4.5.4"
  },
  "dependencies": {
  }
}
